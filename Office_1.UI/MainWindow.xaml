<Window x:Class="Office_1.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Office_1.UI"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800"
        >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="216*"/>
        </Grid.RowDefinitions>
        <Grid Name ="AlLRequestsGrid" Visibility="{Binding RequestsGridVisibility}" Grid.RowSpan="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="75*"/>
                <ColumnDefinition Width="25*"/>
            </Grid.ColumnDefinitions>

            <Grid x:Name="RightOrderGrid" Grid.Column ="2" Background="#FFE0DFDF" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="15*"/>
                    <ColumnDefinition Width="70*"/>
                    <ColumnDefinition Width="15*"/>
                </Grid.ColumnDefinitions>

                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>


                <Button x:Name="NewRequest" Command="{Binding NewRequestCommand}" Content="Новая заявка" Grid.Column="1" Grid.Row="1" Margin="0, 30, 0, 0"/>

                <Label Name="ShowLabel" Content="Показать:" Grid.Column="1" Grid.Row="2" Margin="0, 15, 0, 15" VerticalAlignment="Bottom"/>

                <StackPanel x:Name="CharacteristicsOfOrdersStackPanel" Grid.Row="3" Grid.Column="1" Grid.RowSpan="2" Margin="0, 10, 0, 10">
                    <CheckBox Content="Новые" Margin="0, 5, 0, 0" IsChecked="{Binding ShowNew}"/>
                    <CheckBox Content="На рассмотрении" Margin="0, 5, 0, 0" IsChecked="{Binding ShowInProgress}"/>
                    <CheckBox Content="Рассмотренные" Margin="0, 5, 0, 0" IsChecked="{Binding ShowReviewed}"/>
                    <CheckBox Content="Отменённые" Margin="0, 5, 0, 0" IsChecked="{Binding ShowCanceled}" IsEnabled="{Binding ShowCanceledAvailability}"/>
                </StackPanel>

                <TextBlock Name="AttentionLabel" Text="Отменить можно только новые заявки!" Grid.Column="1" Grid.Row="5" Margin="0, 0, 0, 15" VerticalAlignment="Bottom" TextWrapping="Wrap"/>

                <Button x:Name="CancelRequestButton" Command="{Binding CancelRequestCommand}" Content="Отменить заявку" Grid.Column="1" Grid.Row="6" Margin="0, 0, 0, 30" IsEnabled="{Binding AreButtonsAvailable}"/>
                <Label Visibility="{Binding IsToolTipOn}" Grid.Column="1" Grid.Row="6" Margin="0, 0, 0, 30">
                    <Label.ToolTip>
                        <TextBlock>Для начала выберите заявку, которую хотите удалить, в списке. Отменить можно только новые заявки!</TextBlock>
                    </Label.ToolTip>
                </Label>
            </Grid>


            <Grid x:Name="RequestListGrid" Grid.Column ="0" Background="#FFE0DFDF" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="70*"/>
                    <ColumnDefinition Width="auto"/>
                </Grid.ColumnDefinitions>
                <DataGrid x:Name="OrdersDataGrid" ItemsSource="{Binding Requests}" SelectedItem="{Binding SelectedItem}" CanUserAddRows="False" CanUserDeleteRows="False" CanUserReorderColumns="False" CanUserSortColumns="True"  Grid.Column="0" ColumnWidth="*" AutoGenerateColumns="False">
                    <DataGrid.Columns>
                        <DataGridTextColumn Binding="{Binding Id}" Header="№" IsReadOnly="True"/>
                        <DataGridTextColumn Binding="{Binding Client.Name}" Header="Клиент:" IsReadOnly="True"/>
                        <DataGridTextColumn Binding="{Binding Director}" Header="Руководитель:" IsReadOnly="True"/>
                        <DataGridTextColumn Binding="{Binding Subject}" Header="Тема:" IsReadOnly="True"/>
                        <DataGridCheckBoxColumn Binding="{Binding Status}" Header="Статус:" IsReadOnly="True"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>


        </Grid>



        <Grid Name ="ClientsGrid" Visibility="{Binding ClientsGridVisibility}" Grid.RowSpan="2">

        </Grid>



        <Grid Name ="NewRequestGrid" Visibility="{Binding NewRequestGridVisibility}" Grid.RowSpan="2">

        </Grid>

    </Grid>
</Window>
